require_relative '../lib/app.rb'

describe "shuffle_in_array_and_pick_random_one" do
  it "should return TRUE when 5 is picked up and sent to twtr hdls" do
    it expect(shuffle_in_array_and_pick_random_one(5)).to eq(5) do
    end
  end
end
